require conf/multiconfig/edge-ai-uefi-logo-capsule.inc

# Ensure this variant remains the stock NVIDIA-logo configuration even if the
# parent local.conf selects a different logo for its normal image build.
EDGE_AI_UEFI_LOGO_STYLE = "nvidia"
