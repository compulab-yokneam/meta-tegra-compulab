require conf/multiconfig/edge-ai-uefi-logo-capsule.inc

# Firmware deployed before the machine split records "edge-ai" in its
# TegraPlatformSpec variables. Match that value in the BUP entry TNSPEC.
TNSPEC_MACHINE = "edge-ai"

EDGE_AI_UEFI_LOGO_STYLE = "nvidia"
